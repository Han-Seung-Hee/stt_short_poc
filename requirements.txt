# ==============================================
# Air-Gapped STT + Summarization PoC
# Python 의존성
# ==============================================
# 오프라인 설치 시: pip download -r requirements.txt -d ./wheels
# 내부망에서:      pip install --no-index --find-links=./wheels -r requirements.txt

# === API 서버 ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# === STT (Mac: mlx-whisper) ===
# Mac Apple Silicon 전용. Linux에서는 주석 처리 후 faster-whisper 사용
mlx>=0.5.0
mlx-whisper>=0.4.0

# === STT (Linux CPU/GPU 전용) — Mac에서는 주석 처리 ===
# faster-whisper>=1.0.0

# === NVIDIA GPU 가속 (Linux 필수) ===
# CUDA libcudnn 에러 방지를 위해 GPU 환경이라면 아래 주석을 전면 해제하세요.
# --extra-index-url https://download.pytorch.org/whl/cu118
# torch
# torchvision
# torchaudio

# === 청크 분할 (선택) ===
pydub>=0.25.1

# === LLM 통신 ===
httpx>=0.25.0

# === 설정 ===
pyyaml>=6.0
